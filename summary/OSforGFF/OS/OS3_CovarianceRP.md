# `OS3_CovarianceRP.lean` — Informal Summary

> **Source**: [`OSforGFF/OS/OS3_CovarianceRP.lean`](../../OSforGFF/OS/OS3_CovarianceRP.lean)
> **Generated**: 2026-03-03 00:00
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

Proves reflection positivity for the free covariance: $\mathrm{Re}\langle \Theta f, Cf \rangle \geq 0$
for test functions $f$ supported at positive time $x_0 \geq 0$. The key argument is a
mixed-representation identity derived in `OS3_MixedRep`:
$$\langle \Theta f, Cf \rangle = \frac{1}{2(2\pi)^3} \int_{\bar{k}} \frac{1}{\omega(\bar{k})} \left\lvert \int f(t,\bar{k})\, e^{-\omega t}\, dt \right\rvert^2 d\bar{k} \geq 0,$$
where the factorization $e^{-\omega\lvert x_0 + y_0 \rvert} = e^{-\omega x_0} \cdot e^{-\omega y_0}$
holds because $x_0, y_0 \geq 0$. The two main results are `freeCovariance_reflection_positive_bilinear`
(complex form) and `freeCovariance_reflection_positive_real` (real test function form).

## Status

**Main result**: Fully proven

None — file is sorry-free.

**Length**: 456 lines, 6 definition(s) + 18 theorem(s)/lemma(s)

---

## Reflection Positivity Inner Product

### [`rpInnerProduct`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L55) — Definition

**Lean signature**
```lean
noncomputable def rpInnerProduct (m : ℝ) (f : TestFunctionℂ) : ℂ
```

**Informal**: The reflection-positivity inner product $\langle \Theta f, f \rangle_C = C_\mathbb{C}(\theta f, f)$, defined using the distributional bilinear form with the star (time-conjugation) operation on $f$.

---

## Direct Proof of Reflection Positivity

### [`RPProof.timeReflection`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L70) — Definition

**Lean signature**
```lean
noncomputable def timeReflection (x : SpaceTime) : SpaceTime
```

**Informal**: The time-reflection map $\Theta : (x_0, \vec{x}) \mapsto (-x_0, \vec{x})$ on spacetime, defined locally in the `RPProof` namespace.

---

### [`RPProof.spatialDot`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L76) — Definition

**Lean signature**
```lean
noncomputable def spatialDot (k_spatial x_spatial : SpatialCoords) : ℝ
```

**Informal**: The spatial inner product $\bar{k} \cdot \bar{x} = \sum_i k_i x_i$ between two vectors in $\mathbb{R}^3$.

---

### [`RPProof.freeCovarianceℂ_bilinear`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L79) — Definition

**Lean signature**
```lean
noncomputable def freeCovarianceℂ_bilinear (m : ℝ) (f g : TestFunctionℂ) : ℂ
```

**Informal**: A local copy of the complex bilinear covariance form $\int\!\int f(x)\, C_m(x,y)\, g(y)\, dx\, dy$ in the `RPProof` namespace.

---

### [`RPProof.weightedLaplaceFourier`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L82) — Definition

**Lean signature**
```lean
noncomputable def weightedLaplaceFourier (m : ℝ) (f : TestFunctionℂ) (k_sp : SpatialCoords) : ℂ
```

**Informal**: The mixed Laplace-Fourier transform $F_\omega(\bar{k}) = \int f(x)\, e^{-\omega x_0}\, e^{-i\bar{k}\cdot\bar{x}}\, d^4x$ with $\omega = \sqrt{\lVert\bar{k}\rVert^2 + m^2}$, whose squared modulus appears in the reflection positivity formula.

---

### [`RPProof.rpInnerProduct`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L87) — Definition

**Lean signature**
```lean
noncomputable def rpInnerProduct (m : ℝ) (f : TestFunctionℂ) : ℂ
```

**Informal**: A local copy of the RP inner product $\langle \Theta f, f \rangle_C$ inside the `RPProof` namespace.

---

### [`RPProof.star_apply`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L94) — Lemma

**Statement**: For any complex test function $f$, $(\theta f)(x) = \overline{f(\Theta x)}$, i.e., the star operation applies conjugation after time reflection.

**Proof uses**: *(direct tactic proof)*

---

### [`RPProof.rpInnerProduct_eq_bessel_reflected`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L98) — Theorem

**Statement**: The RP inner product equals
$$\langle \Theta f, f \rangle_C = \int\!\int \overline{f(x)}\, C_m(\Theta x, y)\, f(y)\, dx\, dy,$$
obtained by changing variables $x \mapsto \Theta x$ using the measure-preserving time reflection.

**Proof uses**: [`QFT.timeReflection_measurePreserving`](../../OSforGFF/Spacetime/DiscreteSymmetry.lean#L148),
`MeasurePreserving.integral_comp`

---

### [`RPProof.mixed_representation`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L120) — Theorem

**Statement**: For test functions $f$ supported on positive time, the RP inner product admits the mixed representation
$$\langle \Theta f, f \rangle_C = \frac{1}{2(2\pi)^3} \int_{\bar{k}} \int_x \int_y \overline{f(x)}\, f(y) \cdot \frac{1}{\omega}\, e^{-\omega\lvert{-x_0 - y_0}\rvert} e^{-i\bar{k}\cdot(\bar{x}-\bar{y})}\, dx\, dy\, d\bar{k}.$$

**Proof uses**: [`RPProof.rpInnerProduct_eq_bessel_reflected`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L98),
[`bessel_bilinear_eq_mixed_representation`](../../OSforGFF/OS/OS3_MixedRep.lean#L1676)

---

### [`RPProof.energy_pos`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L135) — Lemma

**Statement**: The relativistic energy $\omega(\bar{k}) = \sqrt{\lVert\bar{k}\rVert^2 + m^2}$ is strictly positive for any spatial momentum $\bar{k}$.

**Proof uses**: *(direct tactic proof)*

---

### [`RPProof.abs_neg_sum_nonneg`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L143) — Lemma

**Statement**: For $x_0, y_0 \geq 0$, we have $\lvert{-x_0 - y_0}\rvert = x_0 + y_0$.

**Proof uses**: *(direct tactic proof)*

---

### [`RPProof.spatialDot_sub`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L148) — Lemma

**Statement**: The spatial dot product is linear in its second argument: $\bar{k} \cdot (\bar{x} - \bar{y}) = \bar{k}\cdot\bar{x} - \bar{k}\cdot\bar{y}$.

**Proof uses**: *(direct tactic proof)*

---

### [`RPProof.exp_spatial_phase_factor`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L156) — Lemma

**Statement**: The spatial phase exponential factorizes: $e^{-i\bar{k}\cdot(\bar{x}-\bar{y})} = e^{-i\bar{k}\cdot\bar{x}} \cdot e^{+i\bar{k}\cdot\bar{y}}$.

**Proof uses**: [`spatialDot_sub`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L148)

---

### [`RPProof.xIntegralFactor`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L162) — Definition

**Lean signature**
```lean
noncomputable def xIntegralFactor (f : TestFunctionℂ) (ω : ℝ) (k_sp : SpatialCoords) : ℂ
```

**Informal**: The $x$-factor $\int \overline{f(x)}\, e^{-\omega x_0}\, e^{-i\bar{k}\cdot\bar{x}}\, dx$ appearing in the Fubini factorization of the RP inner product.

---

### [`RPProof.yIntegralFactor`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L166) — Definition

**Lean signature**
```lean
noncomputable def yIntegralFactor (f : TestFunctionℂ) (ω : ℝ) (k_sp : SpatialCoords) : ℂ
```

**Informal**: The $y$-factor $\int f(y)\, e^{-\omega y_0}\, e^{+i\bar{k}\cdot\bar{y}}\, dy$ appearing in the Fubini factorization.

---

### [`RPProof.xIntegralFactor_eq_conj_neg`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L182) — Lemma

**Statement**: For positive-time test functions, the $x$-factor equals the complex conjugate of the weighted Laplace-Fourier transform evaluated at $-\bar{k}$: $\text{xFactor}_\omega(\bar{k}) = \overline{F_\omega(-\bar{k})}$.

**Proof uses**: [`norm_neg_eq`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L171),
[`spatialDot_neg_left`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L174),
`integral_conj`

---

### [`RPProof.yIntegralFactor_eq_neg`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L206) — Lemma

**Statement**: The $y$-factor equals the weighted Laplace-Fourier transform at $-\bar{k}$: $\text{yFactor}_\omega(\bar{k}) = F_\omega(-\bar{k})$.

**Proof uses**: [`norm_neg_eq`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L171),
[`spatialDot_neg_left`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L174)

---

### [`RPProof.factorization_to_squared_norm_direct`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L228) — Theorem

**Statement**: For positive-time test functions, the mixed-representation double integral over $x, y$ at fixed $\bar{k}$ equals the squared norm:
$$\int_x \int_y \overline{f(x)}\, f(y) \cdot \frac{1}{\omega}\, e^{-\omega\lvert{-x_0 - y_0}\rvert} e^{-i\bar{k}\cdot(\bar{x}-\bar{y})} = \frac{1}{\omega}\, \lvert F_\omega(-\bar{k}) \rvert^2.$$

**Proof uses**: [`abs_neg_sum_nonneg`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L143),
[`exp_spatial_phase_factor`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L156),
[`xIntegralFactor_eq_conj_neg`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L182),
[`yIntegralFactor_eq_neg`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L206),
`MeasureTheory.integral_smul`

---

### [`RPProof.rp_equals_squared_norm_integral`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L322) — Theorem

**Statement**: The RP inner product equals the non-negative spatial momentum integral:
$$\langle \Theta f, f \rangle_C = \frac{1}{2(2\pi)^3} \int_{\bar{k}} \frac{1}{\omega(\bar{k})} \lvert F_\omega(-\bar{k}) \rvert^2\, d\bar{k}.$$

**Proof uses**: [`mixed_representation`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L120),
[`factorization_to_squared_norm_direct`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L228)

---

### [`RPProof.freeCovariance_reflection_positive_direct`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L347) — Theorem

**Statement**: For any complex test function $f$ supported on positive time, the real part of the RP inner product is non-negative: $\mathrm{Re}\langle \Theta f, f \rangle_C \geq 0$.

**Proof uses**: [`rp_equals_squared_norm_integral`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L322),
`MeasureTheory.integral_nonneg`, `Complex.normSq_nonneg`

---

## Bridge to Direct Proof

### [`rpInnerProduct_eq_rpProof`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L381) — Lemma

**Statement**: The outer `QFT` namespace RP inner product coincides with the `RPProof` local version: $\langle \Theta f, f \rangle_C^{\mathrm{QFT}} = \langle \Theta f, f \rangle_C^{\mathrm{RPProof}}$.

**Proof uses**: *(direct tactic proof)*

---

### [`freeCovariance_reflection_positive_bilinear`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L399) — Theorem

**Statement**: For any complex test function $f$ supported at positive time ($x_0 \geq 0$),
$$\mathrm{Re}\langle \Theta f, f \rangle_C \geq 0.$$

**Proof uses**: [`rpInnerProduct_eq_rpProof`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L381),
[`RPProof.freeCovariance_reflection_positive_direct`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L347)

---

## Connection to Real Test Functions

### [`star_toComplex_eq_compTimeReflection`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L411) — Lemma

**Statement**: For real test functions, the star operation on their complexification equals time reflection: $\theta(\mathrm{toComplex}\, f) = \mathrm{compTimeReflection}(\mathrm{toComplex}\, f)$.

**Proof uses**: [`compTimeReflection_toComplex_star_eq`](../../OSforGFF/Covariance/Position.lean#L352)

---

### [`rpInnerProduct_toComplex_eq`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L422) — Lemma

**Statement**: For a real test function $f$, the RP inner product of its complexification equals the bilinear form with time-reflected argument: $\langle \Theta\, \mathrm{toComplex}\, f,\, \mathrm{toComplex}\, f \rangle_C = C_\mathbb{C}(\Theta f, f)$.

**Proof uses**: [`star_toComplex_eq_compTimeReflection`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L411)

---

### [`freeCovariance_reflection_positive_bilinear_real`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L429) — Theorem

**Statement**: For a real test function $f$ supported at positive time,
$$\int\!\int (\Theta f)(x)\, C_m(x,y)\, f(y)\, dx\, dy \geq 0.$$

**Proof uses**: [`freeCovariance_reflection_positive_bilinear`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L399),
[`real_integral_eq_complex_re`](../../OSforGFF/Covariance/Position.lean#L305),
[`rpInnerProduct_toComplex_eq`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L422)

---

### [`freeCovariance_reflection_positive_real`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L450) — Theorem

**Statement**: Alias for `freeCovariance_reflection_positive_bilinear_real`: for positive-time real test functions, $\int\!\int (\Theta_{\mathbb{R}} f)(x)\, C_m(x,y)\, f(y)\, dx\, dy \geq 0$.

**Proof uses**: [`freeCovariance_reflection_positive_bilinear_real`](../../OSforGFF/OS/OS3_CovarianceRP.lean#L429)

---

*This file has **6** definitions and **18** theorems/lemmas (0 with sorry).*
