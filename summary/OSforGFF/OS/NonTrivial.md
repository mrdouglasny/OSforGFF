# `NonTrivial.lean` — Informal Summary

> **Source**: [`OSforGFF/OS/NonTrivial.lean`](../../../OSforGFF/OS/NonTrivial.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

Establishes nontriviality / non-degeneracy of the Gaussian Free Field, closing the loophole
that the OS-axiom verification in `OS.Master` would be satisfied trivially by the Dirac measure
at $\omega = 0$ (the "zero field"). Most of the file is dimension-generic: it works over section
variables `{d : ℕ} [Fact (2 ≤ d)]` with `(m : ℝ) [Fact (0 < m)] [GFFPropagator d m]`, proving,
for every dimension:

1. the square-root propagator embedding $T : S(\mathbb{R}^d) \to L^2$ is injective;
2. the smeared covariance $C(f,f) > 0$ for every nonzero test function $f$;
3. every field pairing $\langle\omega,f\rangle$ has strictly positive variance under the free GFF;
4. the GFF measure is not a Dirac measure ($\mu_{\mathrm{GFF}} \neq \delta_0$).

The injectivity chain is: the Fourier transform is injective on Schwartz space (mathlib's
`FourierPair` left inverse $\mathcal{F}^{-1}\circ\mathcal{F} = \mathrm{id}$), the positive
momentum weight $1/\sqrt{\lVert k\rVert^2 + m^2}$ creates no new zeros, and a continuous function
vanishing a.e. (Lebesgue) vanishes everywhere (`volume` is `IsOpenPosMeasure`).

The final section proves the UV divergence $C(x,y) \to +\infty$ at coincident points, now
**dimension-generic** for $d \ge 2$: `freeCovariance_tendsto_atTop` composes the generic driver
`properTimeCovariance_tendsto_atTop_at_zero` (the proper-time integral $\to +\infty$ as $r \to 0^+$,
from its $t^{-d/2}$ short-time singularity) with $\lVert x_0 - x\rVert \to 0^+$, then identifies the
radial profile with the proper-time integral via `GFFPropagator.schwinger_eq`. It specialises to
$d = 4$ (Bessel kernel), $d = 3$ (Yukawa kernel), and $d = 2$ (the logarithmically divergent $K_0$
kernel), superseding the former 4D-only, `besselK1`-asymptotics proof.

## Status

**Main result**: Fully proven

None — file is sorry-free.

**Length**: 398 lines, 0 definitions + 10 theorem(s)/lemma(s)

---

## Injectivity of the real-to-complex embedding

### [`toComplex_injective`](../../../OSforGFF/OS/NonTrivial.lean#L74) — Theorem

**Statement**: The embedding `toComplex : SchwartzTestFunction d → SchwartzTestFunctionℂ d` is injective (dimension-generic), following from pointwise injectivity of $\mathbb{R} \hookrightarrow \mathbb{C}$.

**Proof uses**: `Complex.ofReal_inj`

---

## Injectivity of the Fourier transform on Schwartz space

### [`fourierTransform_schwartz_injective`](../../../OSforGFF/OS/NonTrivial.lean#L86) — Theorem

**Statement**: The Fourier transform `SchwartzMap.fourierTransformCLM ℂ` on complex Schwartz space is injective, via the `FourierPair` left inverse $\mathcal{F}^{-1}\circ\mathcal{F} = \mathrm{id}$.

**Proof uses**: `FourierTransform.fourierInv_fourier_eq`

---

## Continuous functions that vanish a.e. vanish everywhere

### [`eq_zero_of_continuous_ae_zero`](../../../OSforGFF/OS/NonTrivial.lean#L111) — Lemma (private)

**Statement**: A continuous $f : \mathrm{SpaceTime}\,d \to \mathbb{C}$ that is zero a.e. with respect to Lebesgue measure is zero everywhere: if $f(x_0) \neq 0$ then $f^{-1}(\mathbb{C}\setminus\{0\})$ is open, nonempty, and has positive measure (`volume` is `IsOpenPosMeasure`), contradicting $f = 0$ a.e.

**Proof uses**: `Continuous.isOpen_preimage`, `IsOpen.measure_pos`

---

## Injectivity of the square-root propagator embedding

### [`sqrtPropagatorMap_eq_zero_iff`](../../../OSforGFF/OS/NonTrivial.lean#L133) — Theorem

**Statement**: For a real test function $f$, `sqrtPropagatorMap m f k = 0` for all $k$ iff $f = 0$. Since `sqrtPropagatorMap m f k = 𝓕(toComplex f)(k)·w(k)` with weight $w(k) = $ `freePropagatorMomSqrt d m k > 0`, vanishing of the product forces $\mathcal{F}(\mathrm{toComplex}\,f) = 0$, hence $f = 0$.

**Proof uses**: [`freePropagatorMomSqrt_pos`](../../../OSforGFF/Covariance/ParsevalGeneric.lean#L947),
[`fourierTransform_schwartz_injective`](../../../OSforGFF/OS/NonTrivial.lean#L86),
[`toComplex_injective`](../../../OSforGFF/OS/NonTrivial.lean#L74)

---

### [`embeddingMap_injective`](../../../OSforGFF/OS/NonTrivial.lean#L172) — Theorem

**Statement**: The square-root propagator embedding $T = $ [`embeddingMap m`](../../../OSforGFF/Covariance/RealForm.lean#L327)$: S(\mathbb{R}^d) \to L^2(\mathbb{R}^d,\mathbb{C})$ is injective (dimension-generic). If $T f = T g$ then $\int \lVert\mathrm{sqrtPropagatorMap}\,m\,(f-g)\rVert^2 = 0$; the nonnegative continuous integrand vanishes a.e., hence everywhere, so $\mathcal{F}(f-g) = 0$ and $f = g$.

**Proof uses**: [`embeddingMap_norm_sq`](../../../OSforGFF/Covariance/RealForm.lean#L462),
[`sqrtPropagatorMap_sq_integrable`](../../../OSforGFF/Covariance/RealForm.lean#L141),
[`eq_zero_of_continuous_ae_zero`](../../../OSforGFF/OS/NonTrivial.lean#L111),
[`sqrtPropagatorMap_eq_zero_iff`](../../../OSforGFF/OS/NonTrivial.lean#L133)

---

## Strict positivity of the covariance

### [`freeCovarianceFormR_strictPos`](../../../OSforGFF/OS/NonTrivial.lean#L212) — Theorem

**Statement**: Strict positive-definiteness — $C(f,f) > 0$ for every nonzero real test function $f$, since $C(f,f) = \lVert Tf\rVert^2$ with $T$ injective. This rules out the Dirac-at-zero measure as an OS model.

**Proof uses**: [`freeCovarianceFormR_eq_normSq`](../../../OSforGFF/Covariance/RealForm.lean#L505),
[`embeddingMap_injective`](../../../OSforGFF/OS/NonTrivial.lean#L172)

---

## Nontriviality of the GFF measure

### [`gaussianFreeField_variance_pos`](../../../OSforGFF/OS/NonTrivial.lean#L225) — Theorem

**Statement**: For $f \neq 0$, the pairing $\langle\omega,f\rangle$ has strictly positive variance under the free GFF: $\int (\langle\omega,f\rangle)^2\,d\mu_{\mathrm{GFF}} > 0$ (the pushforward by the pairing is a non-degenerate Gaussian).

**Proof uses**: [`gff_second_moment_eq_covariance`](../../../OSforGFF/Measure/Construct.lean#L297),
[`freeCovarianceFormR_strictPos`](../../../OSforGFF/OS/NonTrivial.lean#L212)

---

### [`gaussianFreeField_not_dirac`](../../../OSforGFF/OS/NonTrivial.lean#L238) — Theorem

**Statement**: The GFF is not a Dirac measure: there exists a test function $f \neq 0$ whose pairing $\langle\omega,f\rangle$ has nonzero variance. A standard smooth bump function witnesses it, so the OS verification in `Master.lean` is nontrivial.

**Proof uses**: `ContDiffBump`, `HasCompactSupport.toSchwartzMap`,
[`gaussianFreeField_variance_pos`](../../../OSforGFF/OS/NonTrivial.lean#L225)

---

## UV divergence: pointwise covariance diverges at coincident points

### [`properTimeCovariance_tendsto_atTop_at_zero`](../../../OSforGFF/OS/NonTrivial.lean#L270) — Theorem

**Statement**: For $d \ge 2$, the proper-time covariance diverges at the origin: $\mathrm{properTimeCovariance}\;d\;m\;r \to +\infty$ as $r \to 0^+$. Lower bound: on the window $[r^2, (4\pi)^{-1}]$ the integrand dominates a constant multiple of $1/t$ (from $e^{-tm^2} \ge e^{-m^2/4\pi}$, $(4\pi t)^{-d/2} \ge (4\pi t)^{-1}$ for $4\pi t \le 1$ and $d \ge 2$, and $e^{-r^2/4t} \ge e^{-1/4}$ for $t \ge r^2$), so the integral grows at least like $\log(1/r^2)$ — the sharp rate at $d = 2$; for $d \ge 3$ the true rate $r^{2-d}$ is polynomial. This is the dimension-generic driver of the UV divergence, replacing the former 4D `besselK1` asymptotics.

**Proof uses**: [`properTime_slice_integrableOn`](../../../OSforGFF/Covariance/ParsevalGeneric.lean#L80), `Real.rpow_le_rpow_of_nonpos`, `tendsto_rpow_neg_nhdsGT_zero`, `setIntegral_ge_of_const_le_real`, `setIntegral_mono_set`

---

### [`freeCovariance_tendsto_atTop`](../../../OSforGFF/OS/NonTrivial.lean#L375) — Theorem

**Statement**: **(Dimension-generic, $d \ge 2$.)** The free covariance diverges at coincident points: $C(x_0, x) = \mathrm{Cprofile}\,\lVert x_0 - x\rVert \to +\infty$ as $x \to x_0$ through $\{x_0\}^c$ — a genuine UV singularity of the free field. Proved by composing `properTimeCovariance_tendsto_atTop_at_zero` with $\lVert x_0 - x\rVert \to 0^+$ and identifying $\mathrm{Cprofile}$ with the proper-time integral via `GFFPropagator.schwinger_eq`; the ambient `[Fact (2 ≤ d)]` supplies the dimension bound. Specialises to $d = 4$ (Bessel kernel), $d = 3$ (Yukawa kernel), and $d = 2$ ($K_0$ kernel); the pointwise regularization $C(x,x) = 0$ is the smeared-theory convention.

**Proof uses**: [`properTimeCovariance_tendsto_atTop_at_zero`](../../../OSforGFF/OS/NonTrivial.lean#L270),
`GFFPropagator.schwinger_eq`, `tendsto_nhdsWithin_of_tendsto_nhds_of_eventually_within`

---

*This file has **0** definitions and **10** theorems/lemmas (0 with sorry).*
