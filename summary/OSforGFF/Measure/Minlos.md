# `Minlos.lean` — Informal Summary

> **Source**: [`OSforGFF/Measure/Minlos.lean`](../../OSforGFF/Measure/Minlos.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

This file applies the proven Minlos theorem (imported from the `bochner` library) to construct
Gaussian measures on the weak dual of a nuclear space. It bridges between the GFF4D project's
nonneg-only positive definiteness (`GFF4D.IsPositiveDefinite`) and the bochner library's
hermitian+nonneg version (`IsPositiveDefinite`), then shows that whenever the covariance form
is realized as a squared norm via a linear map into a Hilbert space, the Minlos hypotheses are
satisfied and a Gaussian probability measure exists.

## Status

**Main result**: Fully proven (0 sorries). Uses external `minlos_theorem` and `minlos_uniqueness`
from the `bochner` library (`Minlos.Main`), and `gaussian_rbf_pd_innerProduct` from
[`GaussianRBF.lean`](../../OSforGFF/General/GaussianRBF.lean).

**Length**: 194 lines, 2 definition(s) + 6 theorem(s)/lemma(s)

---

## GFF4D Positive Definiteness Lemmas

### [`gaussian_positive_definite_via_embedding`](../../OSforGFF/Measure/Minlos.lean#L57) — Lemma

**Statement**: If a covariance form $C : E \times E \to \mathbb{R}$ satisfies
$C(f,f) = \lVert T f \rVert^2$ for a linear map $T : E \to H$ into a real inner product space $H$,
then the Gaussian characteristic functional $f \mapsto \exp\!\bigl(-\tfrac{1}{2} C(f,f)\bigr)$
is positive definite on $E$ in the GFF4D sense.

**Proof uses**: [`gaussian_rbf_pd_innerProduct`](../../OSforGFF/General/GaussianRBF.lean#L232), [`GFF4D.isPositiveDefinite_precomp_linear`](../../OSforGFF/General/PositiveDefinite.lean)

---

## Bridge Lemmas: GFF4D PD to Bochner PD

### [`gff4d_to_bochner_pd`](../../OSforGFF/Measure/Minlos.lean#L97) — Definition

**Lean signature**
```lean
def gff4d_to_bochner_pd {α : Type*} [AddGroup α] {φ : α → ℂ}
    (h_nonneg : GFF4D.IsPositiveDefinite φ)
    (h_symm : ∀ x, φ (-x) = starRingEnd ℂ (φ x)) :
    IsPositiveDefinite φ
```

**Informal**: Promotes a GFF4D nonneg-only positive definite function $\varphi$ to the bochner library's full positive definiteness (hermitian + nonneg), given an explicit symmetry proof $\varphi(-x) = \overline{\varphi(x)}$.

---

### [`conj_cexp_real`](../../OSforGFF/Measure/Minlos.lean#L105) — Lemma *(private)*

**Statement**: The complex exponential of a purely-real complex argument is self-conjugate:
if $\mathrm{Im}(z) = 0$ then $\overline{\exp(z)} = \exp(z)$.

**Proof uses**: `Complex.ext`, `Complex.ofReal_exp`, `Complex.conj_ofReal`

---

### [`gaussian_positive_definite_bochner`](../../OSforGFF/Measure/Minlos.lean#L111) — Lemma

**Statement**: If a covariance form $C$ satisfies $C(f,f) = \lVert T f \rVert^2$ for a linear embedding $T : E \to H$
and $C(-f,-f) = C(f,f)$ for all $f$, then the Gaussian characteristic functional
$f \mapsto \exp\!\bigl(-\tfrac{1}{2} C(f,f)\bigr)$ is positive definite in the bochner sense.

**Proof uses**: [`gff4d_to_bochner_pd`](../../OSforGFF/Measure/Minlos.lean#L97), [`gaussian_positive_definite_via_embedding`](../../OSforGFF/Measure/Minlos.lean#L57), [`conj_cexp_real`](../../OSforGFF/Measure/Minlos.lean#L105)

---

## Gaussian Characteristic Functional

### [`gaussian_characteristic_functional`](../../OSforGFF/Measure/Minlos.lean#L134) — Definition

**Lean signature**
```lean
def gaussian_characteristic_functional
  (covariance_form : E → E → ℝ) (f : E) : ℂ
```

**Informal**: The Gaussian characteristic functional associated to a bilinear covariance form $C$, defined by $\Phi(f) = \exp\!\bigl(-\tfrac{1}{2} C(f,f)\bigr) \in \mathbb{C}$.

---

## Minlos Theorem Applications

### [`minlos_gaussian_construction`](../../OSforGFF/Measure/Minlos.lean#L141) — Theorem

**Statement**: Under the hypotheses that $E$ is a separable nuclear space, $C(f,f) = \lVert Tf \rVert^2$
for a linear map $T : E \to H$, $C(-f,-f) = C(f,f)$, $C(0,0) = 0$, and $f \mapsto C(f,f)$ is
continuous, there exists a probability measure $\mu$ on $\mathrm{WeakDual}\ \mathbb{R}\ E$ satisfying
$$\exp\!\bigl(-\tfrac{1}{2} C(f,f)\bigr) = \int_{\omega} e^{i\langle f,\omega\rangle}\, d\mu(\omega)$$
for all $f \in E$.

**Proof uses**: [`gaussian_positive_definite_bochner`](../../OSforGFF/Measure/Minlos.lean#L111), [`gaussian_characteristic_functional`](../../OSforGFF/Measure/Minlos.lean#L134), `minlos_theorem` (from `Minlos.Main`)

---

### [`gaussian_measure_characteristic_functional`](../../OSforGFF/Measure/Minlos.lean#L165) — Theorem

**Statement**: Under the same hypotheses as `minlos_gaussian_construction`, there exists a
probability measure $\mu$ on $\mathrm{WeakDual}\ \mathbb{R}\ E$ such that for all $f \in E$,
$$\int_{\omega} e^{i\langle f, \omega\rangle}\, d\mu(\omega) = \exp\!\bigl(-\tfrac{1}{2} C(f,f)\bigr).$$

**Proof uses**: [`minlos_gaussian_construction`](../../OSforGFF/Measure/Minlos.lean#L141)

---

## Minlos Uniqueness (re-exported from bochner)

### [`minlos_gaussian_uniqueness`](../../OSforGFF/Measure/Minlos.lean#L184) — Theorem

**Statement**: If two probability measures $\mu_1, \mu_2$ on $\mathrm{WeakDual}\ \mathbb{R}\ E$
both have the same characteristic functional $\Phi$ (which is continuous, positive definite,
and normalized), then $\mu_1 = \mu_2$.

**Proof uses**: `minlos_uniqueness` (from `Minlos.Main`)

---

*This file has **2** definitions and **6** theorems/lemmas (0 with sorry).*
