# `GaussianRBF.lean` — Informal Summary

> **Source**: [`OSforGFF/General/GaussianRBF.lean`](../../../OSforGFF/General/GaussianRBF.lean)
> **Generated**: 2026-03-10
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

This file proves that the Gaussian radial basis function (RBF) kernel
$h \mapsto e^{-\frac{1}{2}\lVert h \rVert^2}$ is a positive definite function on any
real inner product space $H$. The proof proceeds in three steps: (1) the inner product kernel
$\langle x, y \rangle$ is positive definite; (2) exponentiation preserves positive
definiteness via the Hadamard series machinery from `HadamardExp.lean`; (3) the Gaussian kernel
factors as $e^{-\frac{1}{2}\lVert x - y \rVert^2} = e^{-\frac{1}{2}\lVert x \rVert^2}\, e^{-\frac{1}{2}\lVert y \rVert^2}\, e^{\langle x, y \rangle}$,
reducing the RBF to the inner product kernel.

## Status

**Main result**: Fully proven — file is sorry-free.

**Length**: 307 lines, 1 definition(s) + 4 theorem(s)/lemma(s)

---

### [`IsPositiveDefiniteKernel`](../../../OSforGFF/General/GaussianRBF.lean#L35) — Definition

**Lean signature**
```lean
def IsPositiveDefiniteKernel {α : Type*} (K : α → α → ℂ) : Prop
```
**Informal**: A kernel $K : \alpha \times \alpha \to \mathbb{C}$ is positive definite if for every $m \in \mathbb{N}$, every tuple of points $x_1, \dots, x_m \in \alpha$, and every tuple of complex coefficients $c_1, \dots, c_m$, one has $\mathrm{Re}\bigl(\sum_{i,j} \bar{c}_i\, c_j\, K(x_i, x_j)\bigr) \ge 0$.

---

### [`innerProduct_is_pd_kernel`](../../../OSforGFF/General/GaussianRBF.lean#L40) — Lemma

**Statement**: The inner product kernel $K(x, y) = \langle x, y \rangle_{\mathbb{R}}$ (viewed in $\mathbb{C}$) is positive definite on $H$. The proof decomposes coefficients $c_k = a_k + i\,b_k$, rewrites the double sum as $\lVert \sum a_k x_k \rVert^2 + \lVert \sum b_k x_k \rVert^2 \ge 0$.

**Proof uses**: `real_inner_self_nonneg`, `sum_inner`, `real_inner_smul_left`, `real_inner_smul_right`

---

### [`real_valued_PD_kernel_gives_PSD_matrix`](../../../OSforGFF/General/GaussianRBF.lean#L124) — Lemma

**Statement**: If $K$ is a positive definite kernel that is real-valued ($\mathrm{Im}\,K = 0$) and symmetric ($K(x,y) = K(y,x)$), then for any finite tuple of points $x_1, \dots, x_m$, the matrix $M_{ij} = \mathrm{Re}\,K(x_i, x_j)$ is positive semidefinite.

**Proof uses**: [`IsPositiveDefiniteKernel`](../../../OSforGFF/General/GaussianRBF.lean#L35), `Matrix.posSemidef_iff_dotProduct_mulVec`

---

### [`exp_is_pd_kernel`](../../../OSforGFF/General/GaussianRBF.lean#L158) — Lemma

**Statement**: If $K$ is a real-valued symmetric positive definite kernel, then the exponential kernel $(x, y) \mapsto e^{K(x,y)}$ is also positive definite.

**Proof uses**: [`real_valued_PD_kernel_gives_PSD_matrix`](../../../OSforGFF/General/GaussianRBF.lean#L124), [`posSemidef_entrywiseExp_hadamardSeries_of_posSemidef`](../../../OSforGFF/General/HadamardExp.lean#L416), [`entrywiseExp_eq_hadamardSeries`](../../../OSforGFF/General/HadamardExp.lean#L106), `Matrix.PosSemidef.dotProduct_mulVec_nonneg`

---

### [`gaussian_rbf_pd_innerProduct`](../../../OSforGFF/General/GaussianRBF.lean#L232) — Theorem

**Statement**: The function $h \mapsto e^{-\frac{1}{2}\lVert h \rVert^2}$ is a positive definite function on the real inner product space $H$, in the sense of $\mathtt{GFF4D.IsPositiveDefinite}$.

**Proof uses**: [`innerProduct_is_pd_kernel`](../../../OSforGFF/General/GaussianRBF.lean#L40), [`exp_is_pd_kernel`](../../../OSforGFF/General/GaussianRBF.lean#L158), `norm_sub_pow_two_real`, `Complex.exp_add`, `Complex.conj_eq_iff_im`

---

*This file has **1** definition and **4** theorems/lemmas (0 with sorry).*
