# `PositiveDefinite.lean` — Informal Summary

> **Source**: [`OSforGFF/General/PositiveDefinite.lean`](../../../OSforGFF/General/PositiveDefinite.lean)
> **Generated**: 2026-03-10
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

This file defines positive definite functions and proves basic closure properties.
It was extracted from `Minlos.lean` to avoid circular imports with `GaussianRBF.lean`.
The central definition is the standard harmonic-analysis notion: a function
$\varphi : \alpha \to \mathbb{C}$ is positive definite if
$\sum_{i,j} \bar{c}_i c_j \varphi(x_i - x_j) \geq 0$ (in the real part) for every
finite collection of points and complex coefficients.

## Status

**Main result**: Fully proven — file is sorry-free.

**Length**: 55 lines, 1 definition(s) + 1 theorem(s)/lemma(s)

---

## Positive Definiteness

### [`IsPositiveDefinite`](../../../OSforGFF/General/PositiveDefinite.lean#L41) — Definition

**Lean signature**
```lean
def IsPositiveDefinite {α : Type*} [AddGroup α] (φ : α → ℂ) : Prop
```
**Informal**: A function $\varphi : \alpha \to \mathbb{C}$ is positive definite if for every $m \in \mathbb{N}$, every tuple of points $x_1, \dots, x_m \in \alpha$, and every tuple of complex numbers $c_1, \dots, c_m$, the real part of $\sum_{i,j} \bar{c}_i c_j \varphi(x_i - x_j)$ is nonnegative.

---

### [`isPositiveDefinite_precomp_linear`](../../../OSforGFF/General/PositiveDefinite.lean#L47) — Lemma

**Statement**: If $\psi : H \to \mathbb{C}$ is positive definite and $T : E \to_{\mathbb{R}} H$ is a linear map, then the composition $\psi \circ T : E \to \mathbb{C}$ is positive definite.

**Proof uses**: [`IsPositiveDefinite`](../../../OSforGFF/General/PositiveDefinite.lean#L41)

---

*This file has **1** definition and **1** theorem/lemma (0 with sorry).*
