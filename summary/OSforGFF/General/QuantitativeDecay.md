# `QuantitativeDecay.lean` — Informal Summary

> **Source**: [`OSforGFF/General/QuantitativeDecay.lean`](../../../OSforGFF/General/QuantitativeDecay.lean)
> **Generated**: 2026-07-03
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

This file proves that bilinear integrals of Schwartz functions against an exponentially decaying
kernel decay polynomially at any prescribed rate. The main result is
`schwartz_bilinear_translation_decay_polynomial`: for Schwartz functions $f, g$ and a
kernel $K$ satisfying $|K(z)| \leq C_K e^{-m\lVert z \rVert}$ for large $\lVert z \rVert$, there
exists $c \geq 0$ such that
$$\Bigl\lVert \int\!\!\int f(x)\,K(x-y)\,g(y-a)\,dx\,dy \Bigr\rVert \leq c\,(1+\lVert a \rVert)^{-\alpha}$$
for all $a$ and any prescribed $\alpha > 0$. The decay rate and radius are supplied by the caller
(e.g. OS4 passes rate $m/2$ and radius $1$), so the theorems here are stated rate-general. The
proof proceeds through five phases: defining a `PolynomialDecayBound` structure; showing Schwartz
functions satisfy it; converting exponential decay to polynomial decay; proving that convolutions
of polynomial-decay functions again decay polynomially (via a midpoint split); and decomposing
$K = K_{\mathrm{sing}} + K_{\mathrm{tail}}$ to assemble the final bound.

## Status

**Main result**: Fully proven — file is sorry-free.

**Length**: 1055 lines, 7 definition(s) + 3 theorem(s)/lemma(s)

---

## Phase 1: Polynomial Decay Structure and Schwartz Bridge

### [`PolynomialDecayBound`](../../../OSforGFF/General/QuantitativeDecay.lean#L58) — Definition (structure)

**Lean signature**
```lean
structure PolynomialDecayBound {E F : Type*} [NormedAddCommGroup E]
    [NormedAddCommGroup F] (f : E → F) (N : ℝ) where
  C : ℝ
  hC_pos : C > 0
  bound : ∀ x : E, ‖f x‖ ≤ C / (1 + ‖x‖)^N
```
**Informal**: A witness that $f : E \to F$ has polynomial decay of order $N$: a constant $C > 0$ such that $\lVert f(x) \rVert \leq C/(1+\lVert x \rVert)^N$ for all $x$.

---

### [`schwartz_has_polynomial_decay`](../../../OSforGFF/General/QuantitativeDecay.lean#L70) — Definition

**Lean signature**
```lean
def schwartz_has_polynomial_decay (f : SchwartzMap E ℂ) (k : ℕ) :
    PolynomialDecayBound f (k : ℝ)
```
**Informal**: Every Schwartz function $f : E \to \mathbb{C}$ has polynomial decay of every natural number order $k$. The proof extracts the constant from the Schwartz seminorm bound $(1+\lVert x \rVert)^k \lVert f(x) \rVert \leq 2^k \cdot \mathrm{seminorm}_{k,0}(f)$.

---

### [`schwartz_has_polynomial_decay_real`](../../../OSforGFF/General/QuantitativeDecay.lean#L103) — Definition

**Lean signature**
```lean
def schwartz_has_polynomial_decay_real (f : SchwartzMap E ℂ) (N : ℝ) (_hN : N > 0) :
    PolynomialDecayBound f N
```
**Informal**: Schwartz functions have polynomial decay of every positive real order $N$, obtained by taking the natural number ceiling $\lceil N \rceil$ and using monotonicity of the power function.

---

## Phase 2: Exponential to Polynomial Conversion

### [`exp_decay_implies_polynomial_decay`](../../../OSforGFF/General/QuantitativeDecay.lean#L122) — Lemma

**Statement**: For $m, \alpha > 0$, there exists $C > 0$ such that for all $x \geq 0$,
$$e^{-mx} \leq C\,(1+x)^{-\alpha}.$$

**Proof uses**: `ProbabilityTheory.rpow_abs_le_mul_exp_abs`

---

### [`norm_exp_decay_implies_polynomial_decay`](../../../OSforGFF/General/QuantitativeDecay.lean#L161) — Definition

**Lean signature**
```lean
def norm_exp_decay_implies_polynomial_decay {F : Type*} [NormedAddCommGroup F]
    (g : E → F) (m C_exp R₀ : ℝ) (hm : m > 0) (hC_exp : C_exp > 0) (hR₀ : R₀ > 0)
    (hg_decay : ∀ z : E, ‖z‖ ≥ R₀ → ‖g z‖ ≤ C_exp * Real.exp (-m * ‖z‖))
    (hg_bdd : ∃ M : ℝ, ∀ z : E, ‖g z‖ ≤ M)
    (α : ℝ) (hα : α > 0) :
    PolynomialDecayBound g α
```
**Informal**: If $g$ is globally bounded and satisfies $\lVert g(z) \rVert \leq C_{\exp} e^{-m\lVert z \rVert}$ for $\lVert z \rVert \geq R_0$, then $g$ has polynomial decay of every order $\alpha > 0$. The proof splits into a near-origin bound (using global boundedness) and a far-field bound (using the exponential decay via `exp_decay_implies_polynomial_decay`).

---

## Phase 3: Split Convolution Lemma

### [`one_add_half_pow_le`](../../../OSforGFF/General/QuantitativeDecay.lean#L236) — Lemma

**Statement**: For $x \geq 0$ and $N > 0$, $(1 + x/2)^{-N} \leq 2^N (1+x)^{-N}$.

**Proof uses**: `Real.rpow_le_rpow`, `Real.mul_rpow`

---

### [`convolution_polynomial_decay`](../../../OSforGFF/General/QuantitativeDecay.lean#L273) — Definition

**Lean signature**
```lean
def convolution_polynomial_decay
    {u v : E → ℂ} {N : ℝ} (hN_dim : N > Module.finrank ℝ E)
    (hu_decay : PolynomialDecayBound u N)
    (hv_decay : PolynomialDecayBound v N)
    (hu_int : Integrable u) (hv_int : Integrable v) :
    PolynomialDecayBound (fun x => ∫ y, u y * v (x - y)) N
```
**Informal**: If $u$ and $v$ both have polynomial decay of order $N > \dim(E)$ and are integrable, then their convolution $x \mapsto \int u(y)\,v(x-y)\,dy$ also has polynomial decay of order $N$. The proof splits the integration domain at $\lVert y \rVert = \lVert x \rVert / 2$: on the region $\lVert y \rVert \geq \lVert x \rVert / 2$, $u(y)$ is small; on the complementary region, $v(x-y)$ is small (since $\lVert x-y \rVert \geq \lVert x \rVert / 2$). The factor $2^N$ arises from `one_add_half_pow_le`.

---

## Phase 4: Kernel Decomposition Bounds

### [`convolution_compactSupport_decay`](../../../OSforGFF/General/QuantitativeDecay.lean#L437) — Definition

**Lean signature**
```lean
def convolution_compactSupport_decay (f : SchwartzMap E ℂ) (K : E → ℝ) (R₀ : ℝ)
    (hR₀ : R₀ > 0) (hK_loc : LocallyIntegrable K volume)
    (N : ℕ) (_hN : N > 0) :
    PolynomialDecayBound (fun y => ∫ x, f x * (kernelSingular K R₀ (x - y) : ℂ)) (N : ℝ)
```
**Informal**: The convolution of a Schwartz function $f$ with the singular (compactly supported) part $K_{\mathrm{sing}}$ of the kernel has polynomial decay of any natural number order $N$. The proof uses Peetre's inequality: if $K_{\mathrm{sing}}(x-y) \neq 0$ then $\lVert x-y \rVert \leq R_0$, which implies $(1+\lVert y \rVert) \leq (1+\lVert x \rVert)(1+R_0)$ and hence $1/(1+\lVert x \rVert)^N \leq (1+R_0)^N/(1+\lVert y \rVert)^N$.

---

### [`convolution_expDecay_polynomial_decay`](../../../OSforGFF/General/QuantitativeDecay.lean#L614) — Definition

**Lean signature**
```lean
def convolution_expDecay_polynomial_decay (f : SchwartzMap E ℂ) (K : E → ℝ)
    (R₀ m C_K : ℝ) (hR₀ : R₀ > 0) (hm : m > 0) (hC_K : C_K > 0)
    (hK_loc : LocallyIntegrable K volume)
    (hK_decay : ∀ z : E, ‖z‖ ≥ R₀ → |K z| ≤ C_K * Real.exp (-m * ‖z‖))
    (hK_bdd : ∃ M : ℝ, ∀ z : E, |kernelTail K R₀ z| ≤ M)
    (N : ℝ) (hN_dim : N > Module.finrank ℝ E) (hN : N > 0) :
    PolynomialDecayBound (fun y => ∫ x, f x * (kernelTail K R₀ (x - y) : ℂ)) N
```
**Informal**: The convolution of a Schwartz function $f$ with the exponentially decaying tail part $K_{\mathrm{tail}}$ has polynomial decay at any rate $N > \dim(E)$. The proof converts exponential decay of $K_{\mathrm{tail}}$ to polynomial decay via `norm_exp_decay_implies_polynomial_decay`, then applies `convolution_polynomial_decay` after reflecting the kernel.

---

## Phase 5: Main Theorem Assembly

### [`schwartz_bilinear_translation_decay_polynomial`](../../../OSforGFF/General/QuantitativeDecay.lean#L755) — Theorem

**Statement**: Let $f, g$ be Schwartz functions on $E$, and let $K : E \to \mathbb{R}$ be locally integrable with exponential decay $|K(z)| \leq C_K e^{-m\lVert z \rVert}$ for $\lVert z \rVert \geq R_0$. Then for any $\alpha > 0$, there exists $c \geq 0$ such that for all $a \in E$,
$$\Bigl\lVert \int\!\!\int f(x)\,K(x-y)\,g(y-a)\,dx\,dy \Bigr\rVert \leq c\,(1+\lVert a \rVert)^{-\alpha}.$$

**Proof uses**: `convolution_compactSupport_decay`, `convolution_expDecay_polynomial_decay`, `convolution_polynomial_decay`, [`schwartz_bilinear_prod_integrable`](../../../OSforGFF/General/SchwartzTranslationDecay.lean#L402), [`kernel_decomposition`](../../../OSforGFF/General/SchwartzTranslationDecay.lean#L81), `integrable_one_add_norm`, `MeasureTheory.integral_integral_swap`

---

*This file has **7** definitions and **3** theorems/lemmas (0 with sorry).*
