# `GaussianMoments.lean` — Informal Summary

> **Source**: [`OSforGFF/Schwinger/GaussianMoments.lean`](../../OSforGFF/Schwinger/GaussianMoments.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

Proves that the free Gaussian Free Field measure has finite moments of all orders,
establishing integrability of $n$-point correlation functions for complex test functions.
The file is generic in the spacetime dimension: it works under the section variables
`{d : ℕ} [Fact (2 ≤ d)]` with the free covariance supplied by a `[GFFPropagator d m]`
instance, and the measure is `gaussianFreeField_free (d := d) m`.
The central observation is that each pairing $\langle\omega, f\rangle$ is linear in $\omega$
and square-integrable (by the $L^2$ bound from `gaussianFreeField_pairing_memLp`), so
products of two pairings are $L^1$ by Hölder's inequality $L^2 \times L^2 \hookrightarrow L^1$.
The main application is `covariance_bilinear_from_general`, which proves that the
complex two-point Schwinger function of the free GFF is $\mathbb{C}$-bilinear.

## Status

**Main result**: Fully proven

**Major gaps**: None — file is sorry-free.

**Length**: 186 lines, 0 definition(s) + 2 theorem(s)/lemma(s)

---

## n-Point Integrability for Gaussian Free Fields

### [`gaussian_pairing_product_integrable_free_2point`](../../OSforGFF/Schwinger/GaussianMoments.lean#L53) — Theorem

**Statement**: For any two complex test functions $\phi, \psi$ (and $m > 0$ with `[GFFPropagator d m]`), the product $\omega \mapsto \langle\omega,\phi\rangle_\mathbb{C}\cdot\langle\omega,\psi\rangle_\mathbb{C}$ is integrable under the free GFF measure.

The proof decomposes $\phi = \phi_{\mathrm{Re}} + i\phi_{\mathrm{Im}}$ and $\psi = \psi_{\mathrm{Re}} + i\psi_{\mathrm{Im}}$ into real and imaginary parts, applies the $L^2$-integrability of each real pairing via `gaussianFreeField_pairing_memLp`, and uses $L^2 \times L^2 \hookrightarrow L^1$ (Hölder, via `MemLp.integrable_mul`) for each of the four real products, then reassembles the complex product $(ac - bd) + i(ad + bc)$.

**Proof uses**: [`complex_testfunction_decompose`](../../OSforGFF/Spacetime/Basic.lean#L191), [`gaussianFreeField_pairing_memLp`](../../OSforGFF/Measure/Construct.lean#L263), [`distributionPairingCLM_apply`](../../OSforGFF/Spacetime/Basic.lean#L138), `MemLp.integrable_mul`, `MemLp.integrable`, `Integrable.ofReal`, `Integrable.const_mul`, `ENNReal.HolderTriple.mk`, `ENNReal.inv_two_add_inv_two`

---

### [`covariance_bilinear_from_general`](../../OSforGFF/Schwinger/GaussianMoments.lean#L179) — Theorem

**Statement**: The free GFF measure `gaussianFreeField_free (d := d) m` satisfies `CovarianceBilinear`, i.e., the complex two-point Schwinger function $S_2^\mathbb{C}(\phi,\psi) = \int \langle\omega,\phi\rangle_\mathbb{C}\langle\omega,\psi\rangle_\mathbb{C}\,d\mu_m(\omega)$ is $\mathbb{C}$-bilinear in each argument.

**Proof uses**: [`CovarianceBilinear_of_integrable`](../../OSforGFF/Schwinger/Defs.lean#L104), [`gaussian_pairing_product_integrable_free_2point`](../../OSforGFF/Schwinger/GaussianMoments.lean#L53)

---

*This file has **0** definitions and **2** theorems/lemmas (0 with sorry).*
