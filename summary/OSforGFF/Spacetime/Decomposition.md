# `Decomposition.lean` — Informal Summary

> **Source**: [`OSforGFF/Spacetime/Decomposition.lean`](../../../OSforGFF/Spacetime/Decomposition.lean)
> **Generated**: 2026-07-03 (updated for the dimension-generic library)
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

Provides the measure-preserving decomposition $\mathbb{R}^d \simeq \mathbb{R} \times \mathbb{R}^{d-1}$
splitting spacetime into a time coordinate and spatial coordinates, over a generic
dimension `{d : ℕ}` with `[Fact (2 ≤ d)]`.
The equivalence `spacetimeDecomp` is built by composing four measure-preserving steps:
stripping the $L^2$ weighting, reindexing along $d = (d-1)+1$, applying `piFinSuccAbove`,
and restoring spatial $L^2$ structure.
Key consequences include a norm decomposition $\lVert k \rVert^2 = k_0^2 + \lVert k_{\mathrm{sp}} \rVert^2$
and a Fubini-type product-splitting formula for spacetime integrals.

## Status

**Main result**: Fully proven

None — file is sorry-free.

**Length**: 143 lines, 3 definition(s) + 6 theorem(s)/lemma(s)

---

## Integral Decomposition for `SpaceTime d`

### [`piLpMeasurableEquiv`](../../../OSforGFF/Spacetime/Decomposition.lean#L42) — Definition

**Lean signature**
```lean
def piLpMeasurableEquiv (n : ℕ) : PiLp 2 (fun _ : Fin n => ℝ) ≃ᵐ (Fin n → ℝ)
```
**Informal**: The measurable equivalence between the $L^2$-normed product space $\mathrm{PiLp}\,2\,(\mathbb{R}^n)$ and the plain function type $\mathrm{Fin}\,n \to \mathbb{R}$, using the `WithLp` coercion maps.

---

### [`finSplitMeasurableEquiv`](../../../OSforGFF/Spacetime/Decomposition.lean#L48) — Definition

**Lean signature**
```lean
def finSplitMeasurableEquiv : (Fin d → ℝ) ≃ᵐ (Fin (d - 1 + 1) → ℝ)
```
**Informal**: Reindexing along $d = (d-1)+1$ as a measurable equivalence of pi types, via `MeasurableEquiv.piCongrLeft` and `finCongr` (the hypothesis $2 \le d$ guarantees $d = (d-1)+1$).

---

### [`spacetimeDecomp`](../../../OSforGFF/Spacetime/Decomposition.lean#L59) — Definition

**Lean signature**
```lean
def spacetimeDecomp : SpaceTime d ≃ᵐ ℝ × SpatialCoords d
```
**Informal**: The measurable equivalence $\mathbb{R}^d \xrightarrow{\sim} \mathbb{R} \times \mathbb{R}^{d-1}$ decomposing a spacetime point into its time coordinate and spatial coordinates, composed from `piLpMeasurableEquiv`, `finSplitMeasurableEquiv`, `MeasurableEquiv.piFinSuccAbove`, and `MeasurableEquiv.prodCongr`.

---

### [`piLpMeasurableEquiv_measurePreserving`](../../../OSforGFF/Spacetime/Decomposition.lean#L67) — Lemma

**Statement**: The equivalence `piLpMeasurableEquiv n` is measure-preserving with respect to the Lebesgue measure on $\mathrm{PiLp}\,2\,(\mathbb{R}^n)$.

**Proof uses**: `PiLp.volume_preserving_ofLp`

---

### [`spacetimeDecomp_measurePreserving`](../../../OSforGFF/Spacetime/Decomposition.lean#L74) — Theorem

**Statement**: The spacetime decomposition `spacetimeDecomp` is measure-preserving: the pushforward of Lebesgue measure on $\mathbb{R}^d$ is the product Lebesgue measure on $\mathbb{R} \times \mathbb{R}^{d-1}$.

**Proof uses**: [`piLpMeasurableEquiv_measurePreserving`](../../../OSforGFF/Spacetime/Decomposition.lean#L67), `volume_measurePreserving_piCongrLeft`, `measurePreserving_piFinSuccAbove`, `MeasurePreserving.prod`, `MeasurePreserving.id`, `PiLp.volume_preserving_toLp`

---

### [`spacetimeDecomp_apply`](../../../OSforGFF/Spacetime/Decomposition.lean#L101) — Theorem

**Statement**: The decomposition evaluates explicitly as $\mathrm{spacetimeDecomp}(k) = (k_0,\, k_{\mathrm{sp}})$ where $k_0 = k\,0$ and $k_{\mathrm{sp}} = \mathrm{spatialPart}(k)$.

**Proof uses**: *(direct tactic proof)*

---

### [`spacetimeDecomp_symm_eq_spacetimeOfTimeSpace`](../../../OSforGFF/Spacetime/Decomposition.lean#L108) — Lemma

**Statement**: The inverse of `spacetimeDecomp` agrees with `spacetimeOfTimeSpace`: for all $t \in \mathbb{R}$ and $v \in \mathbb{R}^{d-1}$, $\mathrm{spacetimeDecomp}^{-1}(t, v) = \mathrm{spacetimeOfTimeSpace}(t, v)$.

**Proof uses**: `spacetimeDecomp.apply_symm_apply`, [`spacetimeOfTimeSpace_time`](../../../OSforGFF/Spacetime/ProdIntegrable.lean#L41), [`spacetimeOfTimeSpace_spatial`](../../../OSforGFF/Spacetime/ProdIntegrable.lean#L48)

---

### [`spacetime_norm_sq_decompose`](../../../OSforGFF/Spacetime/Decomposition.lean#L129) — Lemma

**Statement**: The squared norm of a spacetime vector decomposes as $\lVert k \rVert^2 = k_0^2 + \lVert k_{\mathrm{sp}} \rVert^2$.

**Proof uses**: `EuclideanSpace.norm_sq_eq`, `Fin.sum_univ_succ`

---

### [`integral_spacetime_prod_split`](../../../OSforGFF/Spacetime/Decomposition.lean#L137) — Lemma

**Statement**: For integrable $f : \mathbb{R} \to \mathbb{C}$ and $g : \mathbb{R}^{d-1} \to \mathbb{C}$, the spacetime integral of a product-type integrand factors:
$$\int_{k \in \mathbb{R}^d} f(k_0)\,g(k_{\mathrm{sp}}) = \left(\int_{k_0 \in \mathbb{R}} f(k_0)\right)\left(\int_{k_{\mathrm{sp}} \in \mathbb{R}^{d-1}} g(k_{\mathrm{sp}})\right).$$

**Proof uses**: [`spacetimeDecomp_measurePreserving`](../../../OSforGFF/Spacetime/Decomposition.lean#L74), [`spacetimeDecomp_apply`](../../../OSforGFF/Spacetime/Decomposition.lean#L101), `integral_prod_mul`

---

*This file has **3** definitions and **6** theorems/lemmas (0 with sorry).*
